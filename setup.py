from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A simple ML inference pipeline that can handle exceptions gracefully',
    author='Chester Cheng',
    license='MIT',
)
